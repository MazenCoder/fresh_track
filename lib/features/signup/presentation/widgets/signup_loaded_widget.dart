import '../../domain/entities/signup_entity.dart';
import 'package:flutter/material.dart';


class SignUpLoadedWidget extends StatelessWidget {
  final SignUpEntity entity;
  const SignUpLoadedWidget({Key? key, required this.entity}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
